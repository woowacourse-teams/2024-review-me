import styled from '@emotion/styled';

export const ConfirmModalMessage = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
  align-items: start;

  min-width: 30rem;

  p {
    width: inherit;
    margin: 0;
  }
`;
