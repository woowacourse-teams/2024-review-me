import styled from '@emotion/styled';

export const Message = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
  align-items: start;

  width: fit-content;
  p {
    width: max-content;
    margin: 0;
  }
`;
