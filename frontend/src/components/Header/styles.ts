import styled from '@emotion/styled';

export const HeaderContainer = styled.header`
  width: 100%;
  height: 3rem;
  display: flex;
  gap: 1rem;
  align-items: center;
  padding-left: 1rem;
  position: fixed;
  top: 0;

  // background-color: #aedaff;

  & > button {
    width: 1.5rem;
    height: 1.5rem;
  }
`;
