import styled from '@emotion/styled';

export const EyeButton = styled.button`
  display: inline-block;

  position: absolute;
  top: calc((100% - 2rem) / 2);
  right: 1rem;

  width: 2rem;
  height: 2rem;

  cursor: pointer;
`;
